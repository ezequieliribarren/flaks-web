<IfModule mod_rewrite.c>
  RewriteEngine On
  # Asegura slash final en directorios (por si el servidor no lo hace solo)
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^(.+[^/])$ $1/ [R=301,L]
</IfModule>

# Permitir PHP para el endpoint de contacto
AddType application/x-httpd-php .php

